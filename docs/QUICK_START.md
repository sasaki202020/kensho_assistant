# Quick Start

## 1. 起動

`start_web_app.bat` をダブルクリックします。

## 2. 確認

ブラウザで `http://127.0.0.1:8787` を開きます。

## 3. 候補更新

`auto_scan.bat` を実行します。

## 4. 応募準備

`/queue` を開いて `Chromeで応募準備` を使います。

## 5. 手動記録

応募ページで自分で送信した場合だけ `手動送信済みにする` を押します。

## 6. 注意

- 応募送信はしません
- 応募送信の自動化はしません
- 最後の送信は人間確認が必要です
